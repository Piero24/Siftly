/**
 * responseParser.ts — Parse and validate LLM responses.
 *
 * Handles JSON extraction from LLM output, schema validation,
 * and conversion to JobApplication format.
 *
 * @see PLAN.md §4 for expected response format.
 */

import type { LLMExtractionResult } from './types';
// import type { JobApplication } from '../../types/job';

// TODO: Phase B — Implement response parsing
//
// export function parseLLMResponse(rawResponse: string): LLMExtractionResult { ... }
// export function validateExtractionResult(result: LLMExtractionResult): string[] { ... }
// export function mergeWithSelectorData(
//   llmData: LLMExtractionResult,
//   selectorData: Partial<JobApplication>
// ): JobApplication { ... }

// Placeholder export.
export const PARSER_VERSION = '0.0.0';
